import { PlagaImagePipe } from './plaga-image.pipe';

describe('PlagaImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlagaImagePipe();
    expect(pipe).toBeTruthy();
  });
});
